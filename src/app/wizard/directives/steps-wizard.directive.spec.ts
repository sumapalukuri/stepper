import { StepsWizardDirective } from './steps-wizard.directive';

describe('StepsWizardDirective', () => {
  it('should create an instance', () => {
    const directive = new StepsWizardDirective();
    expect(directive).toBeTruthy();
  });
});
